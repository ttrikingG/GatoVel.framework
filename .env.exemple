# ===========================================================
#                     CONFIGURAÇÃO DO APP
# ===========================================================

# Nome da aplicação
APP_NAME="MeuFramework"

# Ambiente da aplicação: local, production, staging, etc.
APP_ENV=local

# Habilita ou desabilita debug (true/false)
APP_DEBUG=true

# URL base da aplicação
APP_URL=http://localhost:8000


# ===========================================================
#                  CONFIGURAÇÃO DO BANCO DE DADOS
# ===========================================================

# Tipo de banco de dados a ser usado
# Opções: mysql | pgsql | sqlite
DB_CONNECTION=mysql

# -------------------- MySQL / PostgreSQL -------------------

DB_HOST=127.0.0.1       # Host do banco de dados  
DB_PORT=3306            # Porta do banco (MySQL padrão: 3306, PostgreSQL padrão: 5432)
DB_DATABASE=nome_do_banco  # Nome do banco de dados
DB_USERNAME=usuario_do_banco        # Usuário do banco
DB_PASSWORD=senha_do_banco          # Senha do banco

# ------------------------ SQLite ---------------------------

# Caminho do arquivo do banco SQLite (descomente se usar SQLite)
# DB_SQLITE_PATH=/caminho/para/database.sqlite

# ===========================================================
# OBSERVAÇÕES
# - Sempre mantenha este arquivo fora do controle de versão (.gitignore)
# - As variáveis definidas aqui são carregadas via Dotenv em $_ENV
# - Exemplo de uso: $_ENV['DB_DATABASE'], $_ENV['APP_URL']
# ===========================================================
